{
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ],
  "version": 2,
  "buildCommand": "cd pro && npm install && npm run build",
  "outputDirectory": "pro/dist",
  "installCommand": "cd pro && npm install",
  "framework": "vite"
}